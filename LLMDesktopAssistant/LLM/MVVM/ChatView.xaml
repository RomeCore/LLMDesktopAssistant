<UserControl x:Class="LLMDesktopAssistant.Core.LLM.MVVM.ChatView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:LLMDesktopAssistant.Core.LLM.MVVM"
			 xmlns:ctrl="clr-namespace:LLMDesktopAssistant.Core.Controls"
			 xmlns:loc="clr-namespace:LLMDesktopAssistant.Core.Localization"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Messages Area -->
		<ScrollViewer Grid.Row="0"
					  VerticalScrollBarVisibility="Auto"
					  PanningMode="VerticalOnly"
					  materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
					  ctrl:ScrollViewerBehavior.AutoScrollEnabled="True">

			<ContentControl Content="{Binding MessageSequence}"/>
		</ScrollViewer>

		<!-- Input Area -->
		<Border Grid.Row="1"
				Padding="16,8"
				Background="{DynamicResource MaterialDesignPaper}"
				BorderBrush="{DynamicResource MaterialDesignDivider}"
				BorderThickness="0,1,0,0">

			<ContentControl Content="{Binding UserInput}"/>
		</Border>
	</Grid>
	
</UserControl>