<UserControl x:Class="LLMDesktopAssistant.LLM.MVVM.Attachments.AttachmentsManagerView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:LLMDesktopAssistant.LLM.MVVM.Attachments"
			 xmlns:ctrl="clr-namespace:LLMDesktopAssistant.Controls"
			 xmlns:loc="clr-namespace:LLMDesktopAssistant.Localization"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<Grid Margin="10">

		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListView ItemsSource="{Binding Drafts}">

			<ListView.ItemTemplate>
				<DataTemplate>

					<materialDesign:Card Margin="5" Padding="10">

						<StackPanel>

							<TextBlock Text="{Binding SourceUrl}"
									   FontWeight="Bold"
									   TextTrimming="CharacterEllipsis"/>

							<ComboBox ItemsSource="{Binding AvailableModes}"
									  SelectedItem="{Binding Parameters.Mode}"/>

							<StackPanel Orientation="Horizontal">
								<TextBox Width="60"
										 Text="{Binding Parameters.StartLineIndex}"/>
								<TextBox Width="60"
										 Text="{Binding Parameters.LineCount}"/>
							</StackPanel>

							<StackPanel Orientation="Horizontal">
								<TextBox Width="60"
										 Text="{Binding Parameters.StartByteIndex}"/>
								<TextBox Width="60"
										 Text="{Binding Parameters.ByteCount}"/>
							</StackPanel>

						</StackPanel>

					</materialDesign:Card>

				</DataTemplate>
			</ListView.ItemTemplate>

		</ListView>

		<Button Grid.Row="1"
				Content="Apply"
				Command="{Binding ApplyCommand}"
				HorizontalAlignment="Right"
				Margin="0,10,0,0"/>
	</Grid>

</UserControl>