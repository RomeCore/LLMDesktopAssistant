<UserControl x:Class="LLMDesktopAssistant.LLM.MVVM.Attachments.AttachmentView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:LLMDesktopAssistant.LLM.MVVM.Attachments"
			 xmlns:ctrl="clr-namespace:LLMDesktopAssistant.Controls"
			 xmlns:loc="clr-namespace:LLMDesktopAssistant.Localization"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<materialDesign:Card>

		<DockPanel>

			<Button DockPanel.Dock="Right"
					Style="{StaticResource MaterialDesignIconButton}"
					Command="{Binding RemoveAttachmentCommand}"
					Width="24" Height="24">

				<materialDesign:PackIcon Kind="Remove"
										 Width="16" Height="16"/>

			</Button>

			<TextBlock Text="{Binding Attachment.Title}" Margin="5"/>

		</DockPanel>
		
	</materialDesign:Card>
	
</UserControl>
