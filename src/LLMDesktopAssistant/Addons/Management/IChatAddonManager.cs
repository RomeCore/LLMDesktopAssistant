namespace LLMDesktopAssistant.Addons.Management
{
	/// <summary>
	/// The CHAT-scoped addon manager for managing addons within the chat scope (its workspace).
	/// </summary>
	public interface IChatAddonManager : IAddonManager { }
}
