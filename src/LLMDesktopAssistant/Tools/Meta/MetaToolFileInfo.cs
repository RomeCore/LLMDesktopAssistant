namespace LLMDesktopAssistant.Tools.Meta
{
	/// <summary>
	/// Describes a meta tool file location together with its source.
	/// </summary>
	public sealed record MetaToolFileInfo(string FileName, MetaToolSource Source);
}
